[accounts]
remote.url = "http://0.0.0.0:7799"
lifecycle = "ephemeral"
commit = { frequency-millis = 9_000_000_000_000, compute-unit-price = 1_000_000 }

[ledger]
resume-strategy = { kind = "reset" }

[validator]
millis-per-slot = 50
sigverify = true

[rpc]
port = 8899

[geyser-grpc]
port = 10001

[task-scheduler]
millis-per-tick = 50
path = "test-integration/target/test.db"
reset = true
