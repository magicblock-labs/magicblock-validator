# Agent Skills

This directory is reserved for executable scripts or concrete capabilities agents can run, such as validation helpers, deployment scripts, migrations, or other tool-like workflows.

Do not put general policy, reference context, memory, or specifications here; use `../rules/`, `../context/`, `../memory/`, or `../specs/` instead.
